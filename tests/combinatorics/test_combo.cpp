#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"
#include "cppmath/combinatorics/combo.hpp"

TEST_CASE("Fibonacci numbers") {
    // TODO: Add tests when fibonacci() is implemented
    // CHECK(cppmath::combinatorics::fibonacci(0) == 0);
    // CHECK(cppmath::combinatorics::fibonacci(1) == 1);
    // CHECK(cppmath::combinatorics::fibonacci(10) == 55);
}

TEST_CASE("Factorial") {
    // TODO: Add tests when factorial() is implemented
    // CHECK(cppmath::combinatorics::factorial(0) == 1);
    // CHECK(cppmath::combinatorics::factorial(1) == 1);
    // CHECK(cppmath::combinatorics::factorial(5) == 120);
}

TEST_CASE("Fibonacci sequence") {
    // TODO: Add tests when fibonacci_sequence() is implemented
    // auto seq = cppmath::combinatorics::fibonacci_sequence(100);
    // CHECK(seq.size() > 0);
}

TEST_CASE("Binomial coefficient") {
    // TODO: Add tests when binomial_coefficient() is implemented
    // CHECK(cppmath::combinatorics::binomial_coefficient(5, 2) == 10);
    // CHECK(cppmath::combinatorics::binomial_coefficient(10, 5) == 252);
} 